include(ANHPlugin)

AddANHPlugin(baseline
    DEPENDS 
    	libanh
    	libswganh
	ADDITIONAL_INCLUDE_DIRS
	    ${Boost_INCLUDE_DIR}
        ${GLM_INCLUDE_DIR}
		${GLOG_INCLUDE_DIR}
	    ${TBB_INCLUDE_DIRS}
		${MYSQL_INCLUDE_DIR}
        ${MYSQLCONNECTORCPP_INCLUDE_DIRS}
	ADDITIONAL_LIBRARY_DIRS
	    ${Boost_LIBRARY_DIRS}
		${TBB_LIBRARY_DIRS}
	DEBUG_LIBRARIES 
	    ${TBB_DEBUG_LIBRARIES}
		${GLOG_LIBRARY_DEBUG}
		${MYSQL_LIB}
        ${MYSQLCONNECTORCPP_LIBRARY_DEBUG}
	OPTIMIZED_LIBRARIES 
	    ${TBB_LIBRARIES}
		${GLOG_LIBRARY_RELEASE}
		${MYSQL_LIB}
        ${MYSQLCONNECTORCPP_LIBRARY_RELEASE}
)
